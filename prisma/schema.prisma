generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  discordId String?  @unique
  email     String?  @unique
  aquacoins Int @default(0)
  createdAt DateTime @default(now())

  purchases         Purchase[]
  aquacoins_history AquacoinsHistory[] // Relación inversa añadida aquí
}

model Purchase {
  id        String   @id @default(cuid())
  userId    String
  amountUSD Float
  vbucks    Int
  cashback  Int
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id])
}

model AquacoinsHistory {
  id        String   @id @default(cuid())
  user_id   String
  amount    Int
  createdAt DateTime @default(now())

  user User @relation(fields: [user_id], references: [id])
}
